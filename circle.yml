machine:
  services:
    - docker
  node:
    version: 6.1.0
  environment:
    UT_ENV: circleci

test:
  override:
    - npm run circleci:test

deployment:
  prod:
    branch: master
    commands:
      - npm run circleci:build
      - npm run circleci:push

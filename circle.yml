machine:
  node:
    version: 6.1.0
  environment:
    UT_ENV: circleci

test:
  override:
    - set -o pipefail && npm run test | tap-xunit > ${CIRCLE_TEST_REPORTS}/tape.xml && npm run cover -- --dir ${CIRCLE_ARTIFACTS}

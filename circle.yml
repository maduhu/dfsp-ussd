machine:
  services:
    - docker
  node:
    version: 6.1.0
  environment:
    UT_ENV: circleci

test:
  override:
    - npm run circleci:test

dependencies:
  post:
    - npm run circleci:build

deployment:
  prod:
    branch: master
    commands:
      - npm run circleci:push
